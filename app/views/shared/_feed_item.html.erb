<li id="<%= feed_item.id %>">
  <span class="title"><%= feed_item.title %></span>
  <% if current_user?(feed_item.user) %>
  	<%= link_to "edit", edit_group_path(feed_item.id) %>
    <%= link_to "delete", feed_item, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: feed_item.title %>
    <% end %>
</li>